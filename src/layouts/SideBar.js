import React from 'react';

import '../styles/_SideBar.scss';

const SideBar = () => {
    return (
        <div className="sidebar">
            <div className="activity">Rounded progress bar</div>
        </div>
    );
}

export default SideBar;